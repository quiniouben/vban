# Let's define some things here
AM_CFLAGS = -std=c99 -Wpedantic -Wall -Wno-multichar
AM_LDFLAGS = -lasound

bin_PROGRAMS = vban_receptor
vban_receptor_SOURCES = main.c audio.h audio.c socket.h socket.c vban.h logger.h logger.c
